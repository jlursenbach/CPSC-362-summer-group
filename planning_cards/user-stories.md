
# User Stories:
```
{
  "Name": "Return Stock Data",
  "Primary Actor": "User",
  Description:
    "User inputs a valid stock symbol into a text box. User presses return or     selects a button that activates the text box. The system uses the stock symbol to call yFinance app and return stock data in a JSON. System will display the stock data"
}
```

